{
  "name": "neuroguild-network",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ neuroguild-network",
    "create-local": "graph create --node http://localhost:8020/ neuroguild-network",
    "remove-local": "graph remove --node http://localhost:8020/ neuroguild-network",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 neuroguild-network",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.98.1"
  },
  "overrides": {
    "axios": "^1.8.2",
    "cross-spawn": "^7.0.6",
    "ejs": "^3.1.10",
    "glob": "^11.0.4",
    "js-yaml": "^4.1.1",
    "semver": "^7.7.3",
    "undici": "^7.18.2"
  },
  "devDependencies": {
    "@graphprotocol/graph-ts": "^0.38.2",
    "matchstick-as": "^0.6.0"
  }
}
